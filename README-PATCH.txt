Patched for GitHub Pages:
- Ensured _layouts/default.html has empty front matter only.
- Simplified _config.yml and forced correct url/baseurl.
- Removed .nojekyll if present (Jekyll must run for layouts/markdown).
- Keep all files at REPOSITORY ROOT, not inside a /page folder.
